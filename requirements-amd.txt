# Core ML dependencies for AMD/ROCm systems
# Minimal installation for basic recommendation functionality

# Basic math and data processing
numpy>=1.21.0
pandas>=1.4.0
scikit-learn>=1.1.0
psutil>=5.9.0

# PyTorch with ROCm support
torch --index-url https://download.pytorch.org/whl/rocm6.4
torchvision --index-url https://download.pytorch.org/whl/rocm6.4

# NLP models (will work with CPU fallback)
transformers>=4.20.0
sentence-transformers>=2.2.0

# CPU-based similarity search (works well with AMD)
faiss-cpu>=1.7.0

# Optional advanced features
keybert>=0.7.0
